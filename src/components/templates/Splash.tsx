import { SpotifyLogo } from '../atoms/icons';

export const Splash = () => {
  return (
    <div class="mx-auto flex h-screen w-full max-w-sm items-center justify-center">
      <SpotifyLogo class="w-full text-neutral-200" />
    </div>
  );
};
