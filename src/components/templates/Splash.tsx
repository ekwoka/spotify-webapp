import { SpotifyLogo } from '../atoms/icons';

export const Splash = () => {
  return (
    <div class="mx-auto w-full max-w-sm flex h-screen items-center justify-center">
      <SpotifyLogo class="w-full text-neutral-200" />
    </div>
  );
};
